import { OffenderEvent } from '../../services/appointmentService'

// eslint-disable-next-line import/prefer-default-export
export const offenderEventsMock: OffenderEvent[] = [
  {
    eventDescription: 'Offender Event',
  },
]
