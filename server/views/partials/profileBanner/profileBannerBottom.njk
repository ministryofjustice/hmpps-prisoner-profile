{% from "./macros/alertsMacro.njk" import alertsMacro %}
{% from "./macros/tabLinksMacro.njk" import tabLinksMacro %}

<div class="govuk-grid-row">
  <div class="govuk-grid-column-full hmpps-profile-banner-bottom">
    <div class="govuk-grid-row">
      {{ alertsMacro(alertSummaryData, prisonerNumber, categoryLabel) }}
    </div>
    {% if duplicatePrisonerData and duplicatePrisonerData.length > 0 %}
      {% include "../overviewPage/duplicatePrisonerBanner.njk" %}
    {% endif %}
    <nav class="govuk-grid-row govuk-!-display-none-print">
      <div class="govuk-grid-column-full">
        {{ tabLinksMacro(tabLinks, prisonerNumber) }}
      </div>
    </nav>
  </div>
</div>
