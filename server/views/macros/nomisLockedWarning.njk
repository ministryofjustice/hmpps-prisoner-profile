{% from "moj/components/alert/macro.njk" import mojAlert %}

{% macro nomisLockedWarning(isLocked) %}
    {% if isLocked %}
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-three-quarters">
                {{ mojAlert({
                    variant: "warning",
                    title: "You’re trying to update details that are open in NOMIS",
                    showTitleAsHeading: true,
                    dismissible: false,
                    html: 'To save the details you entered close the NOMIS screen first. Or you can try again later.'
                }) }}
            </div>
        </div>
    {% endif %}
{% endmacro %}
